{
  "description": "DevDog: 문서·주석 작성 후 문체 검사, git commit·gh pr 실행 전 메시지 검사",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/kolint.py\" --hook post",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/kolint.py\" --hook pre",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
